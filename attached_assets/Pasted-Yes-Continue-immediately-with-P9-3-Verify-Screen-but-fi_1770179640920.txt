Yes. **Continue immediately with P9-3 (Verify Screen)** — but first, you must lock the guardrails so “green CI” can’t be achieved by weakening the scanner.

You don’t need permission for more “features.” You need marching orders. Here they are.

---

# ORDER 1 — LOCK THE SCANNER (REQUIRED BEFORE P9-3)

## 1.1 Implement a single explicit allowlist scope

You are allowed to display forbidden-capability terms **only** inside the Halo’s “Forbidden” list.

**Do this exactly:**

* Create file: `src/ui/halo/forbidden-capabilities.ts`
* Export constant: `FORBIDDEN_CAPABILITIES_COPY_ALLOWLIST`
* Populate it with the exact Forbidden list strings.

**Example shape (exact requirement):**

* The forbidden terms must exist **only** in this constant (no duplicates elsewhere).

## 1.2 Update scanner behavior (strict)

Scanner must:

* scan `.tsx/.ts` string literals that can render to users
* ignore HTML attributes (`className`, `aria-*`, `data-*`, etc.)
* fail on forbidden words **anywhere** except:

  * file path matches `src/ui/halo/**`
  * and the string is part of `FORBIDDEN_CAPABILITIES_COPY_ALLOWLIST`

## 1.3 Add two scanner tests (mandatory)

* **Test A (must fail):** put the word `accurate` in a dummy UI string → scanner fails
* **Test B (must pass):** put `truth scoring` only in the allowlisted constant → scanner passes

**If these tests do not exist, the guardrail is not accepted.**

When this is done: commit “Scanner locked + tests added.”

---

# ORDER 2 — IMPLEMENT P9-3 VERIFY SCREEN (THEN STOP)

## 2.1 Route + screen

* Route: `/verify`
* Screen title: `Verify`

## 2.2 Inputs (exact)

* Paste JSON capsule (textarea)
* Upload JSON file (file picker)
* Toggle: **Public verify only**

  * ON → calls public endpoint
  * OFF → calls private endpoint (requires `x-api-key`)

## 2.3 Submit behavior (exact)

On submit:

* show verification result pill (VERIFIED / PARTIALLY / UNVERIFIED)
* show **three cards in exact order**:

  1. Hash
  2. Signature
  3. Chain

Each card must display:

* status label (from API)
* relevant fields (hash algo + c14n method on Hash; signature status + key id on Signature; chain status + previous link on Chain)

## 2.4 Error rendering (exact strings)

* **413** → `Capsule exceeds size cap. Split transcript or use redacted mode.`
* **429** → show Retry-After timestamp and render: `Rate limited. Retry after <timestamp>.`
* **401/403** → `Private endpoints require x-api-key.`

No generic “Something went wrong.”

## 2.5 Halo header

Halo must appear on the verify results view.

## 2.6 Forbidden UI language

Verify screen must not contain any forbidden words outside the allowlisted forbidden-capabilities constant.

---

# ACCEPTANCE CRITERIA (P9-3)

You are done when:

* scanner tests A/B exist and pass correctly
* verify screen renders cards in correct order
* each error status renders exact required string
* Halo is visible on verify results

---

# AFTER P9-3

Stop and report in this format:

* Completed: P9-3 Verify Screen
* Evidence files changed
* Tests added/updated (scanner A/B + verify screen tests)
* CI status

Then I will issue P9-4 orders (Receipts list).

Proceed.
