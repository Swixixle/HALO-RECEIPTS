Accepted. **Scanner lock + P9-3 Verify Screen are APPROVED** *conditional on one correction*: your **public verify routing is wrong as stated** and must be fixed before you proceed to P9-4.

### Why the correction is required

You wrote:

* “Public toggle routes to **GET /api/public/receipts/:id/verify**”

But your P6/P8 public surfaces are:

* **Public proof pack:** `GET /api/public/receipts/:id/proof` (P6.7)
* Public verify is a **read-only verification** and, per your own docs, public endpoints are not “submission paths.”

A Verify screen that accepts pasted capsules and then routes to a public endpoint is fine **only if** “Public verify” means *verify an existing receipt id* (read-only), not submit data.

So, the toggle semantics must be:

* **Private verify** (submission): `POST /api/verify` with x-api-key
* **Public verify** (read-only): user supplies `receipt_id` and you call **either**

  * `GET /api/public/receipts/:id/proof` (preferred, since it is already contract-hardened), or
  * the correct public verify endpoint if you have one (but you must name it exactly)

Right now your statement mixes “verify” with “public submission.” That violates the integration boundary you just documented.

---

# REQUIRED PATCH (do this now, then proceed)

## 1) Rename the toggle label (UI copy)

Change:

* “Public verify only”
  To:
* **“Public proof (receipt id only)”**

This prevents category error and ensures reviewers don’t interpret it as a submission path.

## 2) Change public path to proof pack

Public mode must call:

* `GET /api/public/receipts/:id/proof`

and render the same three cards (Hash/Signature/Chain) from the proof pack fields.

## 3) Input behavior in public mode

In public mode, the Verify screen must show:

* a `receipt_id` input (required)
* hide/disable paste/upload capsule inputs (because public is not submission)

## 4) Tests (add/update)

Add two tests:

* Public mode disables capsule inputs and requires receipt_id
* Public mode calls `/api/public/receipts/:id/proof` and renders cards in correct order

---

# After the patch

Report:

* “P9-3 corrected: public mode uses proof pack”
* tests passing

Then I will issue the next set of direct orders: **P9-4 Receipts List**.

Proceed with the patch now.
