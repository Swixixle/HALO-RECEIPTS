Good. This is exactly the right moment to switch tone from *design* to **command**.

Below is a **DIRECT ORDERS SPEC** you can hand to Rep agents verbatim.
It leaves **zero room for interpretation**, feature creep, or “helpful” deviations.

---

# REP AGENT DIRECT ORDERS

## Project: **Halo UI — Never-Hallucinate Control Layer**

### ABSOLUTE AUTHORITY

You are implementing **exactly** what is written below.
You are **not** designing product behavior.
You are **not** adding features.
You are **not** making UX “improvements.”

If something is not explicitly listed as allowed, **it is forbidden**.

---

## GLOBAL NON-NEGOTIABLE RULES

1. **NO transcript persistence**

   * You may render transcript content if allowed by mode
   * You may NEVER store, cache, log, or rehydrate raw transcripts

2. **NO truth, correctness, intent, or meaning claims**

   * Forbidden words in UI copy:
     `true, false, correct, incorrect, accurate, inaccurate, truth, lie, misleading, verified meaning, factual`
   * “VERIFIED” refers **ONLY** to cryptographic verification per logic table

3. **NO hidden functionality**

   * Every capability must be visible in UI
   * Every forbidden capability must be explicitly stated

4. **Kill switch is absolute**

   * When engaged:

     * Sensors are hidden
     * Interpretations are hidden
     * No LLM calls may be triggered
     * No overrides, no exceptions

5. **Append-only means append-only**

   * No edit
   * No delete
   * No reorder
   * No overwrite

---

## HALO (PERSISTENT HEADER) — MUST EXIST ON ALL RECEIPT PAGES

### Placement

* Fixed top bar
* Visible at all times on:

  * Receipt Viewer
  * Sensors
  * Verify result pages

### HALO BLOCKS (LEFT → RIGHT, EXACT ORDER)

---

### 1️⃣ SYSTEM MODE PILL

**States**

* `KILL SWITCH: ENGAGED`
* `KILL SWITCH: OFF`

**Behavior**

* ENGAGED:

  * Hide Sensors tab
  * Hide Interpretations tab
  * Disable any LLM-triggering UI controls
* OFF:

  * Normal behavior

**Required microcopy (verbatim):**

> “Interpretation and sensor outputs are disabled when the kill switch is engaged.”

---

### 2️⃣ VERIFICATION RESULT PILL (AUTHORITATIVE)

**Allowed values ONLY**

* VERIFIED
* PARTIALLY VERIFIED
* UNVERIFIED

**Computation**

* MUST use backend verification result
* MUST follow exact logic:

  * Hash + Signature + Chain table
* NO UI overrides
* NO client-side recomputation

**Required subtitle (verbatim):**

> “This status reflects cryptographic and chain checks only.”

---

### 3️⃣ CAPABILITY MATRIX MINI-SUMMARY

**Two lists, static, non-interactive**

**Implemented (exact list):**

* Hash verification
* Forensic detectors
* Forensic export
* Signature verification
* Chain verification
* Key governance
* Public verification
* Rate limiting
* API authentication
* Research export
* LLM sensor (observer-only)

**Forbidden (exact list):**

* LLM judgment
* Truth scoring
* Truth arbitration
* Model reconciliation
* Behavioral interpretation

**Link**

* “View full capability matrix”
* Opens static page, read-only

---

### 4️⃣ TRANSCRIPT MODE PILL

**Modes**

* full
* redacted
* hidden

**Tooltip (verbatim):**

> “Transcript mode controls rendering only. Raw transcripts are never persisted.”

---

### 5️⃣ EVIDENCE INTEGRITY POINTER

**Display**

* “Event log: hash-chained”
* Show last line hash (truncated + copy button)

**Behavior**

* Copy button copies full hash
* No edits, no filters

---

## GLOBAL NAVIGATION (EXACTLY 5 ITEMS)

1. Verify
2. Receipts
3. Receipt Viewer
4. Sensors
5. Governance

No additional nav items.
No renaming.
No nesting.

---

## SCREEN-LEVEL ORDERS

---

## 1) VERIFY SCREEN

### Input

* Paste JSON capsule
* Upload JSON file

### Optional Toggle

* “Public verify only”

  * Routes to public endpoint
  * No private auth required

### Output (IMMEDIATE)

* Verification Result Pill
* Three cards, **EXACT ORDER**:

  1. Hash
  2. Signature
  3. Chain

### Failure States (MANDATORY)

* 413 → “Capsule exceeds size cap. Split transcript or use redacted mode.”
* 429 → Show Retry-After timestamp
* 401 / 403 → “Private endpoints require x-api-key.”

No generic error messages allowed.

---

## 2) RECEIPTS LIST

### Columns (EXACT)

* Receipt ID
* Result
* Signature status
* Chain status
* Forensics flags count
* Kill switch icon
* Created timestamp

### Row Actions

* Show verification report
* Open receipt viewer
* Export forensic JSON

**No transcript preview allowed.**

---

## 3) RECEIPT VIEWER (CORE EXPERIENCE)

### Layout

* Two columns ONLY

---

### LEFT COLUMN — MACHINE FACTS (READ-ONLY)

#### A. Verification Panel

* Hash (match/mismatch + algo + canonicalization)
* Signature (status + key ID + issuer)
* Chain (status + previous hash)
* Key governance (ACTIVE / REVOKED / EXPIRED)

#### B. Forensics Panel

* Detectors:

  * risk_keywords
  * high_entropy
  * pii_heuristics

**Required microcopy (verbatim):**

> “Detectors are heuristic signals. They do not imply intent or meaning.”

#### C. Event Log Panel

* Render append-only JSONL
* Collapsed by default
* Copy last hash button

---

### RIGHT COLUMN — HUMAN INTERACTION

#### Tabs (availability conditional)

* Transcript
* Interpretations
* Sensors
* Exports

---

### INTERPRETATIONS TAB (STRICT)

* Append-only entries ONLY
* Entry types:

  * FACT
  * INTERPRETATION
  * UNCERTAINTY

**Required fields**

* Text
* Evidence pointer(s)

**Required microcopy (verbatim):**

> “Interpretations are append-only. Prior entries are never edited.”

---

### KILL SWITCH BEHAVIOR (MANDATORY)

When engaged:

* Replace Interpretations + Sensors tabs with:

> “Kill switch engaged. Interpretations and sensor outputs are blocked.”

Verification, Forensics, Event Log remain visible.

---

## 4) SENSORS SCREEN

### Observation Request Panel

* Observation type dropdown (fixed list)
* Model selector (`provider:model`)
* Button: “Run sensor”

### Output Cards (SCHEMA-BOUND)

Each card MUST show:

* observation_type
* model_id
* Hedging prefix
* Constant confidence statement
* Limitations array (min 2)

**Required microcopy (verbatim):**

> “This output is isolated. The model received only transcript text.”

---

### MULTI-MODEL DISAGREEMENT

* Side-by-side cards
* Banner (verbatim):

> “Disagreement is displayed without resolution by design.”

---

### LANGUAGE HYGIENE ENFORCEMENT

If forbidden language detected:

* Reject request
* Show:

  * Error code: LANGUAGE_VIOLATION
  * Exact blocked tokens
* Provide **template**, NOT rewrite

---

## 5) GOVERNANCE SCREEN

### Key Registry

* Table:

  * key_id
  * issuer_label
  * status
  * valid_from
  * valid_to
  * revoked_reason

**Badge:** “Ed25519 only”

---

### Rate Limits

* Public: 100/min
* Private: 50/min
* Burst: 10/sec

---

### Research Export

* Consent-based only
* Explicit exclusions list:

  * No transcripts
  * No receipt IDs
  * No IPs
  * No exact timestamps
  * No PII values

---

## FINAL WARNINGS TO AGENTS

* Do NOT add analytics
* Do NOT add user tracking
* Do NOT add scoring
* Do NOT “improve wording”
* Do NOT infer intent
* Do NOT hide limitations

If you are unsure: **STOP and ask.**

---

If you want, next I can:

* convert this into **Jira / Linear tickets**
* write the **agent system prompt** that enforces compliance
* generate a **review checklist** to catch violations in PRs

Say the word.
