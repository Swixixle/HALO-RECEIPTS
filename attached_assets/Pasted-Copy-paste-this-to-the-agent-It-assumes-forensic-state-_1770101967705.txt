Copy/paste this to the agent. It assumes /forensic_state/ already exists (as you described) and now we’re upgrading it into a shareable, tamper-evident proof pack.

⸻

AGENT: NEXT INSTRUCTIONS — UPGRADE FORENSIC_STATE INTO SHARE PACK + TAMPER-EVIDENT LOG

Goal

User wants:
	1.	A single folder they can return to and copy/paste to prove the system works.
	2.	No IP leakage (no URLs, no repo identifiers, no secrets, no raw transcripts).
	3.	Ideally it “updates after every action” — implement an appendEvent() utility and wire it into milestone points, plus a verification gate.

You already created /forensic_state/ and P6 evidence. Now implement the next layer:

⸻

A) Add a SAFE “Share Pack” entry point

1) Create /forensic_state/SHARE_PACK.md

Make this the copy/paste doc. Include:
	•	What the system is (1 paragraph)
	•	What it proves (bullets)
	•	What it refuses to do (bullets)
	•	“How to verify” (commands only; no URLs)
	•	Evidence pointers (relative paths only)

Hard bans in this file:
	•	.replit.app, .replit.dev, replit.com
	•	API keys / headers values
	•	any raw transcript content
	•	any private keys / seed strings
	•	any real receipt ids from DB (use synthetic test ids only)

2) Create /forensic_state/REDACTION_RULES.md

Define explicit redactions:
	•	Replace any https://*.replit.app and https://*.replit.dev with [DEPLOYMENT_URL_REDACTED]
	•	Replace IPs with [IP_REDACTED]
	•	Replace x-api-key: * values with [API_KEY_REDACTED]
	•	Replace any “receipt_id” that isn’t in the known synthetic test namespace (e.g. p0-*, p1-*, etc.) with [RECEIPT_ID_REDACTED]

⸻

B) Make EVENT_LOG.jsonl tamper-evident (hash-chained)

1) Implement server/forensic-log.ts

Add:
	•	appendEvent(event)
	•	verifyLogIntegrity()

Format (JSONL) per line:

{
  "ts": "ISO",
  "event_type": "P6_ACCEPTED",
  "summary": "...",
  "evidence_ptrs": ["samples/p6_evidence_packet.md", "forensic_state/EVIDENCE_POINTERS.md"],
  "prev_line_hash": "hex",
  "line_hash": "hex"
}

Hash definition:
	•	canonical = JSON.stringify(line_without_hash_fields)
	•	line_hash = sha256(prev_line_hash + canonical)

2) Add a script gate: script/verify_forensic_state.ts
	•	Recompute chain; fail if any mismatch
	•	Ensure STATE_MANIFEST.md has a timestamp >= last event timestamp (or contains last event hash)

Add npm scripts:
	•	"forensic:verify": "tsx script/verify_forensic_state.ts"

⸻

C) Create an automated “share bundle” (no IP leakage)

1) Add script/build_share_pack.ts

This should:
	•	Create /share_pack/
	•	Copy:
	•	forensic_state/*
	•	samples/p0_evidence_packet.md ... samples/p6_evidence_packet.md (and p5 if present)
	•	Apply redaction rules to ALL .md, .jsonl, .txt in share_pack
	•	Then scan for forbidden strings and fail build if found:
	•	.replit.app, .replit.dev, replit.com
	•	x-api-key
	•	OPENAI_API_KEY
	•	BEGIN PRIVATE KEY
	•	IP regex \b\d{1,3}(\.\d{1,3}){3}\b

Add npm scripts:
	•	"share:build": "tsx script/build_share_pack.ts"
	•	"share:check": "tsx script/build_share_pack.ts --check-only"

Optional: zip it:
	•	/share_pack.zip

⸻

D) Add a snapshot file the user can paste

1) Create script/snapshot_state.ts

Outputs to /forensic_state/SNAPSHOT.txt:
	•	Current phase: P0–P6
	•	TRANSCRIPT_MODE
	•	Counts: receipts / research records / llm observations
	•	Key registry counts by status (ACTIVE/EXPIRED/REVOKED)
	•	Rate limit policy names (NOT secrets)
	•	Latest EVENT_LOG line hash

Add npm script:
	•	"state:snapshot": "tsx script/snapshot_state.ts"

⸻

E) “Update after every action” (practical wiring)

We can’t truly hook “every agent action,” but we CAN wire the real completion points:

Add appendEvent() calls at these milestones:
	•	After P* evidence packet generation completes
	•	After schema migrations
	•	After enabling/disabling kill switch
	•	After share_pack build completes successfully
	•	After publishing (if they do)

If you can’t instrument everywhere quickly:
	•	Add it at the end of each endpoint handler that changes state (e.g., verify/save, kill switch, observe/multi, research export creation).

Also add a single helper:
	•	logMilestone(event_type, summary, evidence_ptrs) calls appendEvent()

⸻

F) Final acceptance checklist (run and confirm)

Run:
	1.	npm run forensic:verify
	2.	npm run state:snapshot
	3.	npm run share:build

Confirm:
	•	/share_pack/ exists
	•	No forbidden strings remain
	•	EVENT_LOG chain verifies

Append event:
	•	P6_SHARE_PACK_READY with evidence pointers to:
	•	forensic_state/SHARE_PACK.md
	•	forensic_state/SNAPSHOT.txt
	•	samples/p6_evidence_packet.md

⸻

Do this first if time is tight:
	1.	Hash-chained EVENT_LOG + forensic:verify
	2.	share:build that redacts & fails on forbidden strings

That gives the user a pasteable proof folder that doesn’t leak IP and is tamper-evident.