This is a well-structured plan to move from "pipeline works" to "deployment-grade anchoring." Let me break this down into concrete tasks and get started.

For the AWS account question: I'll default to separate account as recommended, and include the IAM policy example so a pilot operator can set it up correctly.

Let me start by reading the current state of the relevant files to understand the existing implementation before making changes.

Now let me also read the checkpoint-signer to understand the kid handling:

Now I have a thorough understanding of the codebase. Let me implement all the changes systematically. I'll start with task 1 (anchors=required/optional mode) and work through the list.

Task 1: Wire --anchors=required/optional mode in proof run

Now add anchor mode validation at the start of main and after anchor creation:

Now remove the duplicate anchor initialization that was further down:

Now let me also add the version manifest anchor mode field and validation after anchors are collected:

Now update the version manifest to include anchor mode:

Good. Now let me check LSP to confirm errors are resolved:

Checked code issues
Clean. Now let me move on to Task 2: Harden S3 Object Lock anchor with verified reads, cross-account IAM policy, and governance-mode bypass denial:

Now update the getCheckpointAnchor to accept the new config options:

Now let me move to Task 3: Harden RFC3161 TSA anchor with real token verification, pinned cert allowlist, and imprint validation:

Now update the TSA config in getCheckpointAnchor:

Now let me move to Task 4: Verifier output with anchoring as first-class verdict. I need to update the verifier to show anchor verdicts prominently:

Now let me move to Task 5: Key management hardening - kid required in every checkpoint, verifier refuses to guess keys:

Now I need to add the --strict-kid flag parsing and update the call site:

Now update the help text and the call site where resolveKeyForCheckpoint is used:

Now update the call site where resolveKeyForCheckpoint is invoked to add the strictKid check and kid-required enforcement:

Good. Now let me check LSP:

Checked code issues
Clean. Now let me run the tests to make sure everything still passes:

All 50 tests pass. Now let me run the proof run end-to-end to verify the new features:

Now let me verify the required mode fails correctly:

The --anchors=required mode correctly fails when only log-only is configured. Now let me verify the verifier output with the new verdict format:

The verdict is now first-class with clear integrity and anchoring status. Now let me move to Task 6: Integration tests for anchor failure modes. Let me update the tasks:

Now let me create the integration tests for anchor failure modes. I'll create a new test file:

All 72 tests pass (50 existing + 22 new anchor integration tests). The key rotation proof tests file didn't need updating since it was already separate.

Now let me move to Task 7: Documentation - the "What external anchoring prevents/does not prevent" one-pager and threat model delta:

Now update the THREAT_MODEL.md with the anchoring threat delta and START_HERE.md to reference the new doc:

Now let me add a cross-reference to the anchoring threat delta in the THREAT_MODEL.md:

Now update START_HERE.md to reference the new doc:

Good. Now let me update the replit.md and move to the final task:

Now let me update the replit.md to document the new capabilities:

Now update the testing section in replit.md:

Now update the testing count in replit.md:

Now mark tasks 6 and 7 done and move to the final review:

Now let me run all tests one final time and then submit for architect review:

All 72 tests pass. Now let me submit for architect review:

The architect review passed. Let me mark all tasks as completed:

All 8 tasks are complete. Here's a summary of what was delivered in this sprint:

Deployment-Grade Anchor Hardening -- Complete

All 72 tests pass across 4 test suites. Here's what was built:

Anchor modes: --anchors=required hard-fails in proof runs when only log-only is configured, ensuring production pipelines can't silently skip external anchoring.

S3 WORM hardening: Object Lock now supports both GOVERNANCE and COMPLIANCE retention modes, cross-account IAM, configurable retention days, and stores objectBody/objectHash in proofs for fully offline verification.

RFC3161 TSA hardening: messageImprint is validated as SHA-256 of the anchorHash, trusted TSA certificate fingerprints can be pinned, and offline verification works gracefully when no live token is available.

Verifier verdict: A clear "Verdict" section now shows integrity status (PASS/FAIL) plus anchoring status (LOG-ONLY/PRESENT/NONE) with honest messaging about what each level does and doesn't prove.

Key management: Every checkpoint requires a kid (key identifier), and --strict-kid prevents the verifier from guessing which key to use.

22 anchor integration tests: Comprehensive coverage of hash integrity, tamper detection, S3/RFC3161-specific validations, MultiAnchor fan-out, and required-mode enforcement.

EXTERNAL_ANCHORING.md: One-page reference covering what anchoring prevents and doesn't, the threat model delta (undetectable forgery now requires compromising 4 independent systems instead of 2), minimal IAM policy, and deployment recommendations for pilot through production.