Perfect. You’ve now crossed the real threshold: **“it builds” → “it’s stable under regression pressure.”** With golden tests + `STATE.md` in place, the next work is *operational truth* + *deployment-grade guardrails*.

Here’s the next sprint in the exact order I’d do it, with concrete deliverables.

---

## 1) Add a real health endpoint (stop relying on SPA fallback)

Right now, the SPA fallback returning HTML is masking whether the API is actually alive.

**Deliverable**

* `GET /api/health` → **JSON**, always
* `GET /api/ready` (optional) → checks DB connectivity and returns degraded states

**What it should return**

* `status: "ok" | "degraded"`
* `version` (git SHA or build id)
* `time`
* `db: { ok: boolean }`
* `audit: { headOk: boolean }` (optional, fast check)

**Rule**

* Health endpoints must never require auth (or at least you should have an unauth version for infra checks).

---

## 2) Wire the “Operator-grade verification endpoint” into the product

You already have `verifyAuditChain`. Now expose it explicitly so ops/reviewers can see reality.

**Deliverable**

* `GET /api/audit/verify?limit=1000`
  Returns:

```ts
{
  ok: boolean
  partial: boolean
  totalEvents: number
  checkedEvents: number
  firstBadSeq?: number
  head?: { seq: number; hash: string }
}
```

**UI**

* A single top banner:

  * ✅ Verified (N/N)
  * ⚠️ Verified (N checked / Total M) — “partial coverage”
  * ❌ Verification failed at seq X

This becomes your “instant truth” layer.

---

## 3) CI: prevent regression permanently

Now that tests exist, lock them behind CI so no one can break it later.

**Deliverable**

* GitHub Actions workflow that runs:

  * install
  * typecheck
  * build
  * server tests (`vitest -c vitest.server.config.ts`)
  * lint (if configured)

**Minimum**

* CI must fail on any of those.
* CI should run on PR + main pushes.

---

## 4) Security posture hardening (low effort, high payoff)

You’re at the stage where small “paper cuts” can become real incidents.

**Checklist**

* **Ingress limits:** request body size limits (server + proxy if any)
* **Rate limiting** on auth + key endpoints
* **No secret logging** (audit/log pipeline explicitly redact)
* **Header basics:** `X-Content-Type-Options`, `Referrer-Policy`, etc. (or equivalent via middleware)

**Deliverable**

* `SECURITY.md` with:

  * what you protect
  * what you don’t
  * expected deployment settings

---

## 5) Lock the contract boundaries (so they can’t drift again)

You already validated `observation_type` only at edges.

**Now formalize it**

* Put the mapping in exactly **one** place:

  * `wireToInternalObservation()` and `internalToWireObservation()` (or similar)
* Add a test that fails if someone introduces `observation_type` into internal objects again.

This keeps the camel/snake boundary sacred.

---

## 6) Instrumentation (the “you can actually run this” layer)

You don’t need full observability yet—just truth signals.

**Add structured logs for:**

* audit append (success/failure)
* verify run (ok/partial/fail)
* policy violation (with `observationType`, provider, modelId if available)
* adapter errors

**Add counters (even if just in-memory to start):**

* violations by observationType
* adapter failures by provider
* audit append failures
* verify failures

---

# What I would do next *today*

Given what you reported, the best “next cut” is:

1. Implement `/api/health` (fast)
2. Implement `/api/audit/verify` (fast + high leverage)
3. Add CI workflow (fast + permanent)
4. Add UI banner for verified/partial/failed (medium)

That sequence gives you **operator visibility + non-regression**, which is the “deployment-grade” pivot.

---

## Paste me 2 things and I’ll give you a surgical patch plan (not vague)

1. Your `package.json` scripts section (or just the test command you’re using now)
2. The route file where API endpoints live (or just the audit routes section)

With that, I’ll tell you exactly where to add:

* `/api/health`
* `/api/audit/verify`
* the minimal CI workflow file contents
* the UI banner insertion point (and the exact status logic)
