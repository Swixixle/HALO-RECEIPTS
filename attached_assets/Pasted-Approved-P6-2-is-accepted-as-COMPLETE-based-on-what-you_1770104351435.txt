Approved. **P6.2 is accepted as COMPLETE** based on what you reported.

### Acceptance Notes (why this passes cleanly)

* **Universal naming** is tight and machine-enforceable (`provider:model` + regex + provider allowlist).
* **Single dispatch** via `getAdapterForModelId()` is the right choke point (validation + fetch).
* **Sensor gating** via `ENABLED_SENSORS` is a correct P6 control surface.
* **Stub behavior** returning **501 NOT_IMPLEMENTED** when keys absent is exactly the “safe by default” posture you want in sensor mode.
* **Evidence + tests** are appropriate for an audit trail (`P6_2_ADAPTER_REGISTRY_READY` + last-line hash + 34 tests with negative cases).

No changes required before proceeding.

---

## Proceed to P6.3

### P6.3 Objective (what “done” should mean)

**The system can run an end-to-end “sensor observation” flow using the adapter registry, producing a schema-valid `llm-observation/1.0` artifact, while remaining non-authoritative and kill-switch compliant.**

### P6.3 Required Deliverables (crisp checklist)

1. **Sensor invocation pipeline**

   * Input: transcript (or transcript slice) + `model_id`
   * Path: `getAdapterForModelId(model_id)` → adapter call → normalized observation output
   * Output: strict `llm-observation/1.0` JSON

2. **Normalization contract**

   * A canonical internal shape that every provider adapter must map into
   * Explicit handling for:

     * provider-native “refusal” / “safety block”
     * timeouts / transient errors
     * hard errors (misconfig) vs soft errors (provider unavailable)

3. **Policy enforcement layer (mechanical)**

   * Forbidden words rejection (pre- or post-generation, but deterministic)
   * Auto-hedging prefix if missing
   * Constant confidence statement insertion
   * Mandatory limitations array (min 2)
   * Hard guarantee: **no mutation of verification_status**

4. **Kill switch integration**

   * If kill switch is engaged:

     * observations are **blocked**
     * any cached observation (if caching exists) is **hidden**
     * returns an explicit “observations disabled” condition (not silent)

5. **Evidence & tests**

   * New forensic_state event: `P6_3_SENSOR_PIPELINE_READY`
   * Tests should include:

     * happy path via `mock` adapter (schema-valid output)
     * invalid `model_id` rejection (400)
     * stub providers return 501 unchanged
     * forbidden word → rejected with error
     * missing hedging → auto-prefixed
     * missing limitations → auto-filled to min 2
     * kill switch blocks observations

### Implementation boundary reminder (aligns with your guarantees)

* LLM sees only what you stated: **transcript input** (no receipts, no chain state, no keys).
* Output stays in **observation lane** only; never enters research export.

---

If you implement P6.3 to that bar and report it in the same “Completed Items / Evidence / Event / Hash / Tests” format, I’ll accept or flag deltas immediately.
