Do **not** “implement the full Halo UI” as a new phase yet.

Your next step is **tickets/tasks breakdown first**, and it should be structured so Rep agents can execute *without inventing anything* and so you can **accept/reject via checklists + tests**, not vibes.

Below is the exact plan and the exact ticket set. Copy/paste this into your tracker.

---

# P9 — HALO UI IMPLEMENTATION

## Objective

Ship a UI that **cannot hallucinate features** and **cannot drift from system guarantees**.

## Acceptance Gate

* Halo header present on all receipt-related screens
* All screens match spec exactly
* Kill switch behavior is absolute
* No forbidden words appear anywhere in UI text
* No transcript persistence (no caching, localStorage, analytics, session replay)
* All network calls go through existing endpoints only
* E2E tests cover the non-negotiables

---

# TICKET SET (STRICT, ORDERED)

## P9-0 — Project Constraints Lock (Required)

**Deliverables**

* `docs/UI_NON_NEGOTIABLES.md` created with:

  * forbidden words list
  * kill switch absolute rules
  * transcript rendering vs persistence rule
  * “no new endpoints” rule
* Add repo-level check:

  * a CI test that scans UI strings for forbidden words

**Acceptance**

* CI fails if forbidden words appear in UI copy

---

## P9-1 — Route Map + Nav Shell (5 items only)

**Deliverables**

* Global nav with exactly 5 items:

  1. Verify
  2. Receipts
  3. Receipt Viewer
  4. Sensors
  5. Governance
* No additional nav routes exposed in UI
* Base layout component

**Acceptance**

* Visual inspection + snapshot test of nav order and labels

---

## P9-2 — Halo Header Component (Persistent)

**Deliverables**

* `HaloHeader` component with 5 blocks in exact order:

  1. Kill switch pill + verbatim microcopy
  2. Verification result pill + verbatim subtitle
  3. Capability mini-summary (implemented + forbidden lists) + link
  4. Transcript mode pill + verbatim tooltip
  5. Event log integrity pointer + last-line hash copy button
* Must render on:

  * Receipt Viewer
  * Sensors
  * Verify results page

**Acceptance**

* Component tests validate:

  * order of blocks
  * exact strings (verbatim)
  * copy button copies full hash
* Snapshot tests for all states:

  * kill switch engaged/off
  * each verification state
  * each transcript mode

---

## P9-3 — Verify Screen (Deterministic)

**Deliverables**

* Input methods:

  * paste JSON capsule
  * upload JSON file
* Toggle:

  * “Public verify only” routes to public endpoint
* Output:

  * verification pill
  * 3 cards in exact order: Hash → Signature → Chain
* Error states with exact text:

  * 413, 429 (with Retry-After), 401/403

**Acceptance**

* Integration tests:

  * verifies card order
  * verifies exact error messages per status code
  * confirms private endpoint requires x-api-key (UI surfaces correctly)

---

## P9-4 — Receipts List Screen (Truth-free triage)

**Deliverables**

* Table columns EXACT:

  * Receipt ID
  * Result
  * Signature status
  * Chain status
  * Forensics flags count
  * Kill switch icon
  * Created timestamp
* Row actions:

  * Show verification report
  * Open receipt viewer
  * Export forensic JSON
* Explicitly NO transcript preview

**Acceptance**

* UI tests confirm:

  * column order and presence
  * row expansion shows only allowed actions
  * transcript never appears

---

## P9-5 — Receipt Viewer Screen (Two-column)

**Deliverables**

* Left column panels (read-only):

  1. Verification Panel
  2. Forensics Panel + verbatim microcopy
  3. Event Log Panel (collapsed by default)
* Right column tabs:

  * Transcript
  * Interpretations
  * Sensors
  * Exports
* Interpretations tab:

  * append-only entries
  * type dropdown: FACT / INTERPRETATION / UNCERTAINTY
  * required evidence pointer(s)
  * verbatim microcopy
* Kill switch behavior:

  * replaces Interpretations+Sensors tabs with the block message (verbatim)
  * Proof/Forensics/Event Log still visible

**Acceptance**

* Component tests confirm:

  * panels exist and are read-only
  * interpretations have no edit/delete
  * kill switch engaged hides the two tabs and shows the banner
* E2E tests validate kill switch behavior

---

## P9-6 — Sensors Screen (Observer-only)

**Deliverables**

* Observation request panel:

  * observation type dropdown (fixed list)
  * model selector provider:model
  * Run sensor button
* Output cards show required fields:

  * observation_type, model_id
  * hedging prefix
  * constant confidence statement
  * limitations array (min 2)
  * verbatim microcopy
* Multi-model disagreement view:

  * side-by-side cards
  * banner verbatim
* Language violation:

  * shows LANGUAGE_VIOLATION
  * lists exact blocked tokens
  * shows template (no rewrite)

**Acceptance**

* UI tests for:

  * schema field presence
  * disagreement banner exact text
  * language violation rendering rules
* Kill switch engaged disables this entire screen’s actions

---

## P9-7 — Governance Screen

**Deliverables**

* Key registry table:

  * key_id, issuer_label, status, valid_from, valid_to, revoked_reason
  * “Ed25519 only” badge
* Rate limits panel:

  * Public 100/min, Private 50/min, Burst 10/sec
* Research export:

  * consent-based
  * explicit exclusions list (exact)

**Acceptance**

* Snapshot tests verify all displayed values and labels

---

## P9-8 — Frontend “No Persistence” Enforcement

**Deliverables**

* Explicitly forbid:

  * localStorage/sessionStorage use
  * analytics trackers
  * session replay tools
  * caching transcripts
* Add ESLint rule or CI grep checks for:

  * `localStorage`, `sessionStorage`
  * known analytics SDK imports
* Ensure fetch calls don’t log request/response bodies

**Acceptance**

* CI fails if forbidden persistence patterns appear

---

## P9-9 — E2E Test Pack (Non-negotiables)

**Deliverables**
Cypress/Playwright tests covering:

1. Halo header appears on required pages
2. Kill switch engaged blocks sensors + interpretations
3. Transcript hidden mode never renders transcript
4. Forbidden words never appear in UI
5. Verify errors render exact messages
6. Proof pack view includes proof_scope_excludes and never includes transcript

**Acceptance**

* Tests run in CI and must pass

---

# EXECUTION INSTRUCTIONS TO REP AGENTS (copy/paste)

* Implement tickets **in order** P9-0 → P9-9.
* Do not create new endpoints.
* Do not change any server behavior.
* Do not add any new UI copy beyond the spec.
* If a data field is missing from an API response, STOP and file a ticket; do not infer it.

---

## What you should do right now

Pick one:

1. **“Start P9 with P9-0 through P9-2 only”** (best: gets Halo shipped fast)
2. “Implement all tickets sequentially” (fine, slower review)

My recommendation: **Start with P9-0 → P9-2**.
Once Halo is real and persistent, everything else is straightforward.

If you want, paste what stack you’re using for frontend (React/Next/Vite?) and test runner (Playwright/Cypress?), and I’ll tailor the ticket wording to your actual repo layout without changing scope.
