P7 is **ACCEPTED as COMPLETE**, with one explicit condition: **capture the closure artifacts in the same evidentiary form as P6** (event name + full 64-hex hash + a short “wired into runtime paths” evidence list). Your narrative indicates you *did* the wiring pass, but you haven’t posted the **forensic_state event + hash** yet.

### Why I’m accepting (substance check)

Based on what you reported, you hit the P7 acceptance targets:

* **P7.1 Controls Matrix**: all 11 threats covered + mitigations + tests (29 passing).
* **P7.2 Input hardening**: middleware applied to public routes + tests; you adjusted the oversize test to validate Content-Length deterministically (good move—no flaky giant payloads).
* **P7.3 Proof pack misuse guardrails**: `proof_scope` fields added + tests updated.
* **P7.4 Prompt injection detection**: implemented as **non-binding flags** plus enforced non-authoritative outputs; wired into observation and multi-model observe endpoints.
* **P7.5 Observability without leakage**: security audit events with 19 tests; then you explicitly **wired them into runtime** (rate limiter, kill switch, forbidden words, etc.).
* **Full suite**: **170 tests passing** (P6: 113 + P7: 57).

That is a legitimate phase completion.

---

## Required closure (do this now to finalize the audit trail)

Post this block (copy/paste style), and then P7 is *fully* closed:

1. **Event name** (I recommend):

* `P7_0_THREAT_MODEL_HARDENED`

2. **Full last-line hash** (64 hex)

3. **Runtime wiring evidence (short list)**
   Example format:

* `routes.ts`: inputHardening applied to public verify + proof pack
* `rateLimiter`: logs RATE_LIMIT_EXCEEDED event
* `sensor pipeline`: logs FORBIDDEN_WORDS + PROMPT_INJECTION_FLAGGED (flag only)
* `kill switch`: logs KILL_SWITCH_ENGAGED on block paths

No need to paste code—just file/function anchors.

Once you provide that event+hash, **P7 is sealed**.

---

## Next: Publish path (no new scope creep)

Given P6–P7 are now solid, the next step is **P8 — Public Release Readiness** (not “more features,” just shipping hardening).

### P8.1 Release checklist (minimum)

* Versioned schemas published: `ai-receipt/error/1.0`, proof pack schema, transcript-mode contract `1.1`
* Public docs: “what this proves / what it does not prove” (include `proof_scope_excludes`)
* Security defaults: kill switch documented + tested behavior
* Rate limit policy documented (limits + headers + Retry-After)
* Red-team notes: known limitations + non-goals

### P8.2 Launch artifacts

* README “how to verify a receipt”
* Example proof pack payload (sanitized)
* Minimal curl examples for:

  * `/verify`
  * `/proof`
  * error envelope example (404 + 429)

---

## Answer to your question

You asked: **review a specific control or proceed to publish?**

**Proceed to publish**—after you paste the **P7 event + full hash**.
If you want one targeted review anyway, the two highest-impact checks are:

1. **Error oracle leakage** (details fields never leak key/DB info)
2. **Prompt injection detection is truly non-binding** (flag only; doesn’t become a “score”)

Drop the P7 closure event+hash and we move to P8.
