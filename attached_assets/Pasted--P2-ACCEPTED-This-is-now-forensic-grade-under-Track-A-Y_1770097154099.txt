✅ P2 ACCEPTED. This is now forensic-grade under Track A.

You delivered exactly what was required: corrected chain anchoring, removed silent bypass, added explicit chain fields, tightened status logic, and—most importantly—posted raw evidence A–E in chat.

⸻

Acceptance Notes (why it passes)

Chain design is now defensible
	•	You moved to receipt_hash_sha256 = SHA256(c14n(capsule_core)) with a clearly defined core field set.
	•	Test A proves LINKED + link_match true with a real prior hash.
	•	Test D proves BROKEN forces UNVERIFIED.

Signature handling is now unambiguous
	•	No SKIPPED.
	•	UNTRUSTED_ISSUER leads to PARTIALLY_VERIFIED when hash+chain are good (Test B).
	•	INVALID forces UNVERIFIED (Test C).

Verification_status logic behaves correctly under composition
	•	hash_match: false would dominate (you didn’t include a hash-mismatch P2 test, but P0 already proved that path; still fine).
	•	Chain status properly influences final status (Test D).
	•	Genesis + no signature is PARTIALLY_VERIFIED (Test E).

⸻

Two minor cleanups (non-blocking, but do them now)

1) Chain “expected vs observed” naming

In Test A you include expected_previous_hash and observed_previous_hash. Good.

But define them precisely in code/docs:
	•	observed_previous_hash = the value present in the submitted receipt (previous_receipt_hash_sha256)
	•	expected_previous_hash = what the verifier computed as the actual prior receipt’s receipt_hash_sha256

Right now the labels are easy to swap mentally. Make it explicit.

2) Canonicalization must be version-locked

You already include canonicalization method in the core. Ensure:
	•	c14n_version is included in capsule_core
	•	verifier refuses unknown versions (UNVERIFIED + UNKNOWN_CANONICALIZATION)

This prevents “change canonicalization method, keep fields same” ambiguity.

⸻

P3 Authorization (next stage)

✅ Authorized to proceed to P3.
P3 is still crypto hardening, but now we shift from “it works” to “it survives the real world.”

P3 Scope (do not exceed)

P3.1 Key registry governance (trust lifecycle)

Add:
	•	status: ACTIVE | REVOKED | EXPIRED
	•	valid_from, valid_to (optional but recommended)
	•	issuer_label (human readable)
	•	revoked_reason

Verifier rules:
	•	If key is REVOKED/EXPIRED → treat as UNTRUSTED_ISSUER (or introduce REVOKED_ISSUER if you want, but you don’t need a new enum if you include reason).

P3.2 Key rotation support

Allow multiple keys for the same issuer label, different key IDs.
	•	Show issuer identity and key id in verify-result.

P3.3 Public verification endpoint discipline

Add a public verify route that can be shared safely:
	•	returns verify-result + forensics + chain summary
	•	respects TRANSCRIPT_MODE
	•	never returns raw transcript in “public” mode unless explicitly configured

⸻

Copy/paste directive to your Replit agent (P3 kickoff)

Send verbatim:

Proceed with P3 only. Implement key registry governance: keys have status ACTIVE/REVOKED/EXPIRED plus reason metadata; verification treats revoked/expired as untrusted with explicit reason. Add key rotation support (multiple keys per issuer). Add a public verification endpoint that returns verify-result + chain + forensics but strictly respects TRANSCRIPT_MODE and never leaks raw transcript in public mode. Add tests: revoked key, expired key, rotated key valid, and public endpoint transcript redaction/hidden enforcement. Do not add LLMs or schema v1.1.

⸻

If you want, I’ll also give you the exact P3 evidence packet format now (like P0/P1/P2) so the agent can’t “handwave approval” again.