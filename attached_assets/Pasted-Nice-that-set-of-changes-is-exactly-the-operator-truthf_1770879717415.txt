Nice — that set of changes is exactly the “operator-truthful” split you want. With liveness/readiness/audit-verify now contract-stable and tests locking the wire boundary, the next work is no longer “TypeScript correctness”; it’s **operational integrity + adoption ergonomics**.

Here’s the next sprint, in priority order, with concrete deliverables.

---

## 1) Add **one** end-to-end “audit chain smoke” test (API-level)

You have golden/unit tests; now you need a *single* integration test that proves the system works through HTTP.

**Goal:** catch “route + auth + DB + serialization” failures that unit tests can’t.

**Test outline**

1. Create a user/session (or use a test auth bypass if you have one).
2. POST 2–3 events that append to the audit chain.
3. Call `/api/audit/verify?limit=1000`
4. Assert:

   * `ok === true`
   * `partial === false`
   * `checkedEvents >= 2`
5. Mutate one row directly (or simulate by inserting a bad hash if you have a test-only hook).
6. Verify:

   * `ok === false`
   * `firstBadSeq` is set

**Deliverable:** `tests/integration/audit.e2e.test.ts` (or similar) + CI runs it.

---

## 2) Make `/api/ready` **cheap and non-flappy**

Readiness must be meaningful but not noisy.

**Check now**

* Are you doing a full audit verify in readiness? Don’t.
* Readiness should do:

  * DB query (e.g., `SELECT 1`)
  * Audit head existence + shape check (not full chain verification)
  * Optional: “audit head table reachable” and “latest head row readable”

**Add anti-flap**

* If audit head check fails but DB is up, consider returning:

  * `status: "degraded"`, `ready: true` (depending on your ops philosophy)
* Or if you want strict readiness, keep `503` but ensure the “audit head failure” is rare and deterministic.

**Deliverable:** readiness semantics explicitly documented + stable.

---

## 3) Add **runtime metrics** (minimal, high-value)

You now have the right endpoints; next is visibility.

At minimum, instrument:

* `/api/ready` latency + result (`ok/degraded/down`)
* `/api/audit/verify` latency + `checkedEvents` + `partial` + `ok`

If you don’t want a full Prometheus setup yet, just do structured logs:

* `route`, `ms`, `ok`, `partial`, `checkedEvents`, `totalEvents`, `firstBadSeq`

**Deliverable:** consistent log fields that make debugging and dashboards trivial.

---

## 4) Lock down **auth + error shapes** across *all* `/api/*`

Your note about `res.status(code)` after `res.json()` is the right catch—now do the same consistency sweep everywhere.

**Standardize**

* Every error response is JSON with:

  * `error: { code, message, requestId? }`
* Never return HTML from API routes (even on 404)
* Ensure auth failures are always `401` and never leak stack traces

**Deliverable:** `apiError()` helper + consistent error contract.

---

## 5) Performance and safety: make verify scalable and abuse-resistant

You already have a server-side cap; now add the two practical protections:

### A) Rate limiting (even light)

* per user
* per IP (optional)
* especially for `/api/audit/verify`

### B) Cursor-based verification option (optional but powerful)

If the audit log can grow, add:

* `?fromSeq=...&toSeq=...`
  so you can do incremental verifies and avoid long scans.

**Deliverable:** verify remains safe under load and large history.

---

## 6) UI: turn readiness + audit state into a “no-myth” banner

Now that `AuditBanner` reads `/api/ready`, tighten the semantics:

Show:

* ✅ Ready: DB ok + audit head ok
* 🟡 Degraded: DB ok, audit head not ok (or other)
* 🔴 Down: DB not ok

And for audit chain:

* show last verify time + partial vs full coverage
* explicitly label “partial verification” when `partial=true`

**Deliverable:** operators never guess what the system knows.

---

## 7) Documentation: promote contracts to “non-negotiable truth”

Update `STATE.md` (you already did) but also add:

* `docs/API_CONTRACTS.md` (or `docs/ops.md`) with:

  * `/api/health` (liveness)
  * `/api/ready` (readiness)
  * `/api/audit/verify` (integrity verification)
  * explicit *non-goals* (semantic truth, completeness of external systems, etc.)

**Deliverable:** procurement / security review has something canonical to cite.

---

# If you want the cleanest next move

Do **#1 (E2E audit chain smoke test)** first. It’s the highest leverage because it validates the entire stack and will pay dividends every time you refactor routes/storage/auth.

If you paste your current route file names (just the paths), I’ll give you an exact, minimal integration test scaffold that matches your project structure and auth model.
